# ml/evaluation/

The evaluation harness: precision/recall/F1 benchmark scripts run against published baselines from the spoofing-detection literature. Built in Weeks 5-6 of `ImplementationPlans/Sem5IP.md` before any model training starts, so every later iteration has a number attached.
