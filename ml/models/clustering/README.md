# ml/models/clustering/

Fleet-wide DBSCAN clustering, to separate area jamming (many vessels affected at once) from targeted spoofing (one vessel affected).

This is a Stage 2 item (MIP section 8.3) — directory is scaffolded now so the Stage 1 model layout matches the target repo structure, but no implementation lands here until Stage 2.
