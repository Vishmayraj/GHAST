# data/raw/

Archived raw AIS messages, as received from the collector before normalization.

This directory is gitignored: raw messages are large and reprocessing-only, so they live in S3-compatible object storage (MinIO locally, per `infra/docker/`), not in git. This README exists so the directory itself stays tracked.
