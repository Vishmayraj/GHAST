# data/research_datasets/

Public, labeled research datasets used for training and evaluation:

- IEEE DataPort synthetic GPS spoofing dataset.
- Real-vs-simulated spoofed AIS track studies.

These are downloaded via the evaluation harness setup, not committed directly (large files); this directory holds the local copies once fetched.
