# docs/

The design doc (`HLD_and_Master_Implementation_Plan.md`) already lives at `HLD/` at the repo root, established in the initial commit — that stays the single source of truth rather than duplicating it here.

`research_notes/` holds consensus/red-team findings as they come in (MIP section 9 risks).
